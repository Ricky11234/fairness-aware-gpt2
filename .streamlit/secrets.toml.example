# Copy to .streamlit/secrets.toml for local runs, or paste into
# Streamlit Cloud → your app → Settings → Secrets.
# Without it the dashboard and swap preview still work; only live
# predictions are disabled.
MODEL_REPO = "your-hf-username/fairness-gpt2-qqp"
